<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="StyleText">
        <item name="android:textSize">@dimen/tv_size</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">casual</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/colorText</item>
    </style>

    <style name="StyleTextDescription">
        <item name="android:textSize">@dimen/tv_description_size</item>
        <item name="android:fontFamily">casual</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="StyleTextInfo">
        <item name="android:textSize">@dimen/tv_size</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">casual</item>
        <item name="android:layout_marginLeft">@dimen/row_layout_margin</item>
        <item name="android:textColor">@color/colorText</item>
    </style>

    <style name="StyleTextInfoDescription">
        <item name="android:textSize">@dimen/tv_description_size</item>
        <item name="android:fontFamily">casual</item>
        <item name="android:layout_marginLeft">@dimen/row_layout_margin</item>
    </style>

</resources>
